import { FunctionComponent } from "react";
import styles from "./styles.module.scss";

export const LocationFilter: FunctionComponent = () => {
  return <button className={styles.locationFilter}>Location</button>;
};
